#include<bits/stdc++.h>
using namespace std;

int main(){
    // lower_bound and upper_bound function does not works, rest all functions are same as above it does not stores in any particulear order it has a better complexity then set in most cases, except some when collision happens -> it hold the unique property but it is not in a sorted manner.
    return 0;
}